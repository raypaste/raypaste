{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "overlay",
  "description": "Capability for overlay notification and progress windows",
  "windows": ["notification-*", "progress-*"],
  "permissions": [
    "core:default",
    "core:window:allow-close",
    "core:event:allow-emit"
  ]
}
