{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "overlay",
  "description": "Capability for overlay notification and progress windows",
  "windows": ["notification-*"],
  "permissions": [
    "core:default",
    "core:window:allow-close"
  ]
}
